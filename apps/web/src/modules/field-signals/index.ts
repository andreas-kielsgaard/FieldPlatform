export function getSignalsIndexView() {
  return {
    title: "Field signals",
    summary:
      "Signals are time-sensitive field context, kept separate from feeds and generic notifications.",
  };
}
