# Project Setup Integration Review

## Status

Deferred.

## Note

Project-specific setup material has been moved under `Agent operating system migration/docs/agent/project-setup/`.

The new technology architecture map is a project-specific architecture spine, not a generic Agent OS component. Later migration work should review how this folder is referenced by task modes, structural-maintenance behavior, generated indexes, and human-facing documentation.

The review should decide whether project setup files are:

- read directly only for architecture-shaping tasks
- queried through tools
- referenced by structural-maintenance behavior
- promoted into a different project-specific context system
- kept separate from recursively maintained Agent OS surfaces
